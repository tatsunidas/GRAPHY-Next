# fixtures/video-mp4-avi

NonDicomImporter用の動画（MP4 H.264、および非H.264/AVIの両方を推奨）。

- 最低ファイル数: 1
- 実データはこのフォルダ直下に置く（サブフォルダ可）。git管理対象外（.gitignore）。
- 配置確認: `npm run automator -- check-fixtures --category video-mp4-avi`
