# fixtures/image-png-jpg-bmp-gif-tif

NonDicomImporter用の静止画像（png/jpg/bmp/gif/tif）各種。

- 最低ファイル数: 1
- 実データはこのフォルダ直下に置く（サブフォルダ可）。git管理対象外（.gitignore）。
- 配置確認: `npm run automator -- check-fixtures --category image-png-jpg-bmp-gif-tif`
