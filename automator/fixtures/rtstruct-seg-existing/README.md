# fixtures/rtstruct-seg-existing

既存のRTSTRUCT/DICOM SEGファイル一式（元画像シリーズ含む）。読込・往復検証用。

- 最低ファイル数: 1
- 実データはこのフォルダ直下に置く（サブフォルダ可）。git管理対象外（.gitignore）。
- 配置確認: `npm run automator -- check-fixtures --category rtstruct-seg-existing`
