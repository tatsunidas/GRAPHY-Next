# fixtures/xa-no-geometry

XA/DX/CRシリーズ（ImageOrientationPatient/ImagePositionPatient無し）。Fusion非空間フォールバック検証用。

- 最低ファイル数: 1
- 実データはこのフォルダ直下に置く（サブフォルダ可）。git管理対象外（.gitignore）。
- 配置確認: `npm run automator -- check-fixtures --category xa-no-geometry`
