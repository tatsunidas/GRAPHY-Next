# fixtures/mr-siemens-mosaic

MRシリーズ（Siemens MOSAIC形式）。デモザイク処理の検証用。

- 最低ファイル数: 1
- 実データはこのフォルダ直下に置く（サブフォルダ可）。git管理対象外（.gitignore）。
- 配置確認: `npm run automator -- check-fixtures --category mr-siemens-mosaic`
