# fixtures/single-slice-localizer

単一スライス/localizer/scoutシリーズ。3D不可ガード・GridView無効化条件の検証用。

- 最低ファイル数: 1
- 実データはこのフォルダ直下に置く（サブフォルダ可）。git管理対象外（.gitignore）。
- 配置確認: `npm run automator -- check-fixtures --category single-slice-localizer`
