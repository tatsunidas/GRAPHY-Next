# fixtures/ct-basic

最も単純なCTシリーズ（複数スライス、等間隔、チルトなし）。 検証の最初の縦串（DBリセット→import→MainScreen検索→2Dビューア表示）の主データ。

- 最低ファイル数: 1
- 実データはこのフォルダ直下に置く（サブフォルダ可）。git管理対象外（.gitignore）。
- 配置確認: `npm run automator -- check-fixtures --category ct-basic`
