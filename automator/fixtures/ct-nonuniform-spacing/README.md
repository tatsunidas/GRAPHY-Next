# fixtures/ct-nonuniform-spacing

CTシリーズ（非等間隔スライス、または複数収集/複数オリエンテーション混在）。 5D ZCT判定・純スタックフォールバックの検証用。

- 最低ファイル数: 1
- 実データはこのフォルダ直下に置く（サブフォルダ可）。git管理対象外（.gitignore）。
- 配置確認: `npm run automator -- check-fixtures --category ct-nonuniform-spacing`
