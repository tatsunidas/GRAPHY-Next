{
  "name": "graphy-next-plugin-uvs",
  "version": "0.2.0",
  "private": true,
  "description": "UVS (fetal echo video summarization) plugin for GRAPHY-Next — UI face",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "verify": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "esbuild": "^0.23.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
