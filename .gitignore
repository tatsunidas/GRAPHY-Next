# --- Java / Maven ---
backend/target/
backend/src/main/resources/static/

# --- Node / frontend ---
node_modules/
frontend/node_modules/
frontend/dist/
frontend/portable-dist/
desktop/node_modules/
# frontend-maven-plugin が配置する node ランタイム
frontend/node/

# --- Electron build staging / output ---
desktop/renderer/
desktop/resources/backend/
desktop/resources/jre/
desktop/resources/ffmpeg/
desktop/resources/dcm4che/
desktop/dist/

# --- IDE ---
.vscode/
.idea/
*.iml
.settings/
.classpath
.project

# --- ユーザーマニュアル (mkdocs) のビルド成果物 ---
/site/

# --- misc ---
*.log
.DS_Store

# standalone ランタイム生成物（H2 索引 / 受信 DICOM）
backend/data/
/data/

# dcm4chee compose のランタイムデータ
deploy/dcm4chee/data/
deploy/demo/data/

# Cloudflare Tunnel トークン（コミット禁止。.env.example のみ追跡）
deploy/demo/.env

# Electron 起動時(CWD=desktop)に backend が作るランタイムデータ
desktop/data/
# プラグインの導入先（graphy.plugins.dir=./plugins が CWD 相対で作られる）と台帳
desktop/plugins/
# 外部 AI の API キー保管庫（OS キーチェーンが使えない環境でここへ暗号化して置く）。
# 🔴 実行時データであってソースではない。中身は暗号化済みだが、鍵の持ち回りを
#    前提にした設計なのでリポジトリには決して入れない。
desktop/secrets.enc.json
# Art of Imaging が生成した作品が CWD=desktop に残ったもの
desktop/graphy-art-*.png

# 個人メモ（リポジトリに含めない）
memorandum_76.txt
CLAUDE.md

# 社内向け価格プラン（非公開・リポジトリに含めない）
PRICING-PLAN.md
